---
import Image from 'astro/components/Image.astro';
import temperature from '../../public/temperature.png';
import wind from '../../public/wind.png';
import leaflet from '../../public/leaflet.png';
import Main from '../layouts/Main.astro';
---

<Main>
  <section class="intro-section">
    <h1>Visualizing MET Tile Data</h1>
    <p>
      This documentation provides examples of how to visualize temperature and wind data from MET's tile server. Two
      examples use WebGL for rendering data with shaders, while the third demonstrates rendering raw wind data within a
      Leaflet <code>GridLayer</code> using a 2D canvas.
    </p>
  </section>

  <section class="examples-section">
    <div class="example">
      <a href={`${import.meta.env.BASE_URL}/wind`}>
        <div class="example-content">
          <Image class="example-image" src={wind} alt="Wind data visualization using WebGL" loading="eager" />
          <div class="example-text">
            <h2>Wind</h2>
            <p>WebGL-rendered wind data visualization.</p>
          </div>
        </div>
      </a>
    </div>

    <div class="example">
      <a href={`${import.meta.env.BASE_URL}/temperature`}>
        <div class="example-content">
          <Image
            class="example-image"
            src={temperature}
            alt="Temperature data visualization using WebGL"
            loading="eager"
          />
          <div class="example-text">
            <h2>Temperature</h2>
            <p>WebGL-rendered temperature data visualization.</p>
          </div>
        </div>
      </a>
    </div>

    <div class="example">
      <a href={`${import.meta.env.BASE_URL}/map`}>
        <div class="example-content">
          <Image
            src={leaflet}
            class="example-image example-image--leaflet"
            alt="Leaflet example rendering wind data"
            loading="eager"
          />
          <div class="example-text">
            <h2>Leaflet Example</h2>
            <p>Rendering raw wind data in a Leaflet <code>GridLayer</code> using a 2D canvas.</p>
          </div>
        </div>
      </a>
    </div>
  </section>
  <p>
    The entire repository with all the code needed to run these examples can be found on <a
      target="_blank"
      rel="noopener"
      href="https://github.com/nrkno/yr-map-docs">GitHub</a
    >.
  </p>

  <style is:global>
    body {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      font-family: Arial, sans-serif;
      margin: 0;
    }

    .intro-section {
      margin-bottom: 40px;
    }

    .examples-section {
      display: flex;
      gap: 40px;
      justify-content: center;
      align-items: center;
    }

    .example {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .example-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      border: 2px solid lightgrey;
      outline: 1px solid transparent;
      border-radius: 12px;

      overflow: hidden;
      width: 300px;
      position: relative;

      &:hover {
        border-width: 2px;
        border-color: darkgrey;
        outline-color: darkgrey;
      }
    }

    .example-image {
      height: 150px;
      width: 100%;
      object-fit: cover;
    }
    .example-text {
      border-top: 1px solid darkgrey;
      min-height: 100px;
    }

    .example-image--leaflet {
      object-fit: scale-down;
    }

    .example h2 {
      margin: 16px 0;
    }

    .example a {
      text-decoration: none;
      color: inherit;
    }

    .example a:hover h2 {
      color: blue;
      text-decoration: underline;
    }
  </style>
</Main>
